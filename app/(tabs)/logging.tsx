import React from 'react';
import { View } from 'react-native';
import CalendarComponent from "@/components/calendarComponent";

const Logging = () => {

    return (
        <View className=" flex-1 bg-surface_a0">
            <CalendarComponent />
        </View>
    );
};

export default Logging;
